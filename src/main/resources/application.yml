server:
  port: 8080
  #servlet:
  #  context-path: /security
spring:
  application:
    name: springsecuritydevops
  datasource:
    url: jdbc:mysql://localhost:3306/worlddb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  security:
    user:
      name: maheedhar
      password: password

